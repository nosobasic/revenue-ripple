# Apply from terraform-practice, not here

This folder is leftover. Revenue Ripple AWS is applied only from:

```bash
cd /Users/donte/terraform-practice/stacks/revenue-ripple/prod
terraform apply
```

See `terraform-practice/README.md` and `CUTOVER.md` in this folder for the send/cutover steps.
